# 依存・ビルド生成物 (web/nes.{js,wasm} はビルド環境なしで配信するため意図的にコミット)
node_modules/
dist/
m5stack/.pio/
m5stack/compile_commands.json
# cargo は成果物を作業ツリーに置くしかない (verify のように mktemp へ逃がせない)
cli/target/

# nix / direnv
.direnv/
result

# ROM・TASムービー (ローカル取得物。再配布しない)
*.nes
*.fm2

# 秘密情報 (雛形は secrets.h.example)
m5stack/src/secrets.h

# Python
__pycache__/
.venv/

# macOS
.DS_Store
m5stack/.cache/
